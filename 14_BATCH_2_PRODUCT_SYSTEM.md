# Batch 2 product system

> SUPERSEDED HISTORICAL SNAPSHOT. This file records the Batch 2 milestone. Its runtime counts, cache version, state schema, sign split, audio behavior and readiness claims are not current. Use `17_CYCLE_1_REMEDIATION.md`, current data and current code.

## Historical scope

Batch 2 introduced the first-run equipment profile, a Today route, due-first cards, an early ELP flow, situation modes, equipment comparison, a document wallet, mobile navigation and an offline shell. It did not establish the final Cycle 1 contract.

The original Batch 2 verification used 16 FHWA SVG files and treated the other 64 sign entries as text drills. That split is obsolete. Current data contains 49 official FHWA SVG, 15 variable-local training cards and 16 `TRAINING DMS`.

The old ELP Step 1 action merely opened practice and did not enforce a real pass gate. Current Step 1 requires five fixed independent responses. Step 2 remains disabled until pass and then includes all 49 official SVG plus all 16 `TRAINING DMS`.

The bank of 75 items is now named representative training prompts. It is not an official standardized question set.

## Superseding Cycle 1 changes

- Semantic content IDs with `contentVersion` 2 and a checked-in map for all 640 legacy IDs.
- State schema 3 with deep normalization, main and backup recovery, quarantine and safe import.
- Mastery based on two independent unsupported retrievals at least 24 hours apart.
- Three separate applicability profiles across every main collection.
- Typed slots with separate display and spoken forms.
- Explicit professional quotas instead of positional `[:400]` selection.
- Full 49, 15 and 16 sign provenance split with corrected legends and alt text.
- A 75-row listening matrix with distinct clean, phone and roadside assets and an explicit limitation.
- Current versioned shell and media caches with stale cache cleanup and Range support.
- Local server CSP, Permissions-Policy and other defensive headers.

## Historical evidence status

Any Batch 2 screenshots, browser checks or cache names prove only the old milestone. They must not be cited as proof of the current app. Current verification commands and limitations are in `17_CYCLE_1_REMEDIATION.md`.
